# Dependencies / build output
node_modules/
dist/
out/
*.tsbuildinfo

# Runtime state & SECRETS — never commit these
switchboard-config.json
switchboard-users.json
FIRST-RUN-ADMIN-PASSWORD.txt
logs/
*.jsonl
*.log

# Anything that looks like a credential or key
*apikey*
*api_key*
*.pem
*.key
*secret*

# Code-signing certs — private keys, NEVER commit
certs/
*.pfx
*.p12
*.pfx.b64

# OS / editor cruft
.DS_Store
Thumbs.db
.vscode/
.idea/
